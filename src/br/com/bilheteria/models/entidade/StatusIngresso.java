package br.com.bilheteria.models.entidade;

// Este enum agora é público e está em seu próprio arquivo.
public enum StatusIngresso {
    // Estes são os valores corretos que o resto do seu código espera.
    VALIDO,
    UTILIZADO,
    CANCELADO;
}